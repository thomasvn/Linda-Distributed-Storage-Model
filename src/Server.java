public class Server {
    // Create a Socket()

    // Bind() the socket

    // Tell server to Listen

    // While Loop
        // Accept()
        // "Linda>" should be available
            // Accepts commands "in, rd, out, add"
        // Server should also always be listening for commands sent to it
}
